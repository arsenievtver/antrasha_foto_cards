# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.env
backend/.env
backend/var/
backend/data/
key.json
**/__pycache__/

dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Активный nginx-шаблон — это сгенерированный артефакт (HTTP- или TLS-источник).
# Кладёт его update.sh / tls-enable.sh. Не отслеживается, чтобы git pull
# не конфликтовал с локальным состоянием на сервере (TLS-overlay).
deploy/nginx/templates/default.conf.template

# Локальные выгрузки (Excel и т.п.) — не коммитить
exports/
*.xlsx
*.xls
